# Dedicated process for recurring maintenance, monitoring, discovery and cache warming jobs.
# HTTP remains available for platform health checks, but public traffic should target the API role.
storesight.runtime.role=scheduler
server.port=${SERVER_PORT:8081}
