spring:
  config:
    activate:
      on-profile: worker
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

storesight:
  runtime:
    role: scraper-worker
